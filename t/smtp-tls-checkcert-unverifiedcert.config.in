[multimailhook]
	mailer = smtp
	smtpServer = smtp.gmail.com # Host that works with SSL
	smtpencryption = tls
	smtpUser = matthieu.moy
	smtpPass = secret # Your password here
	smtpCACerts = ../orphan-certificate.pem # None server certificate is signed by this CA
